#include <stdlib.h>
int main ()
{ 
 int**arr=(int**)malloc (m*sizeof(int*))
  for (int i=0;i<m;i++)
  { *(arr+i)=(int*)malloc(n*sizeof(int));
   for (int j=0;j<n;j++) *(*(a+i)+j)
   { scanf("%d",*(a+i)+j);
   }
  } 
   for(int i=0,i<m;i++)
   { for (int i=0,j<n;j++)
    printf("%d",*(*(a+i)+j));
   }
   printf("\n");
   return 0;
}
